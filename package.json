{
  "name": "muffin",
  "version": "0.0.1",
  "license": "MIT",
  "main": "dist/app.js",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && tsc",
    "start": "node dist/app.js"
  },
  "dependencies": {
    "@slack/bolt": "^3.13.1",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
